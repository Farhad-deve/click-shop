export interface Category {
    id: string;
    name: string;
    description: string;
    image: string;
}

export interface CategoryResponse {
    id: string;
    name: string;
    description: string;
    image: string;
}
